import items from "../resource/items.js";

// https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Global_Objects/Array/forEach

// TODO: products 브랜드, index 출력

items.forEach((el, idx) => {
    console.log(el.브랜드, idx);
});